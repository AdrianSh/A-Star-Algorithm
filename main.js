window.onload = () => {
    Assistent();
    /*
    Assistent.numRows = 12;
    Assistent.numColumns = 12;
    Assistent.builder = new Builder();

    Assistent.builder.map.rect(Assistent.builder.map.terrains.bridge, 8, 10, 4, 1);
    Assistent.builder.map.origin = { x: 2, y: 2 };
    Assistent.builder.map.destiny = { x: 11, y: 4 };
    Assistent.builder.map.render();
    */
}
